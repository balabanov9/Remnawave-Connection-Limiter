# Remnawave Connection Limiter

Скрипт для контроля количества одновременных подключений к VPN на базе панели [Remnawave](https://github.com/remnawave/backend).

Если юзер подключается с большего количества устройств (IP), чем разрешено в его подписке (`hwidDeviceLimit`) — подписка временно отключается.

## Как это работает

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│   VPN Node  │     │   VPN Node  │     │   VPN Node  │
│  (Finland)  │     │  (Poland)   │     │  (Germany)  │
└──────┬──────┘     └──────┬──────┘     └──────┬──────┘
       │                   │                   │
       │    POST /log      │                   │
       └───────────────────┼───────────────────┘
                           │
                           ▼
                 ┌─────────────────┐
                 │  Central Server │
                 │  (this script)  │
                 └────────┬────────┘
                          │
                          │ API calls
                          ▼
                 ┌─────────────────┐
                 │    Remnawave    │
                 │     Panel       │
                 └─────────────────┘
```

1. На каждой VPN ноде запущен `node_reporter.py` — он парсит логи Xray и отправляет информацию о подключениях на центральный сервер
2. Центральный сервер собирает данные со всех нод и считает уникальные IP для каждого юзера за последние 60 секунд
3. Если количество IP превышает лимит (`hwidDeviceLimit`) — юзер блокируется через API Remnawave на 2 минуты
4. После истечения блокировки юзер автоматически разблокируется

### Оптимизация памяти

- **Node Reporter** читает логи раз в 60 секунд (не постоянно)
- Сохраняет позицию последней прочитанной строки в `/tmp/node_reporter_state.json`
- При перезапуске продолжает с сохраненной позиции (не перечитывает весь лог)
- Автоматически определяет ротацию лога и начинает сначала
- Отправляет данные пачкой через `/log_batch` (меньше запросов)
- Дедуплицирует записи перед отправкой

- **Центральный сервер** агрессивно чистит базу каждые 30 секунд
- Удаляются записи старше 2 минут
- База остается маленькой (несколько MB вместо гигабайтов)

## Требования

- Python 3.10+
- Панель Remnawave 2.0+
- Доступ к логам Xray на нодах

## Установка

### 1. Центральный сервер

Сервер, на котором будет крутиться основной скрипт. Может быть тот же сервер, где панель Remnawave.

```bash
# Клонируем репозиторий
git clone https://github.com/balabanov9/Remnawave-Connection-Limiter.git
cd remnawave-connection-limiter

# Устанавливаем зависимости
pip install -r requirements.txt

# Копируем и настраиваем конфиг
cp config.example.py config.py
nano config.py
```

**Настройки в `config.py`:**

```python
# URL панели Remnawave (без /api на конце)
REMNAWAVE_API_URL = "http://localhost:3000"

# Bearer токен из панели (Настройки -> API Tokens)
REMNAWAVE_API_TOKEN = "your_api_token_here"

# На каком порту слушать логи от нод
LOG_SERVER_PORT = 5000

# Как часто проверять подключения (секунды)
CHECK_INTERVAL_SECONDS = 30

# Окно времени для подсчета уникальных IP (секунды)
IP_WINDOW_SECONDS = 60

# На сколько блокировать при превышении лимита (секунды)
BLOCK_DURATION_SECONDS = 120

# Telegram уведомления (опционально)
TELEGRAM_BOT_TOKEN = "123456:ABC-DEF..."  # от @BotFather
TELEGRAM_CHAT_ID = "123456789"  # твой личный Telegram ID
```

**Запуск:**

```bash
python main.py
```

### 2. VPN ноды

На каждой VPN ноде нужно запустить `node_reporter.py`.

```bash
# Копируем скрипт на ноду
scp node_reporter.py root@your-node:/opt/

# На ноде редактируем настройки
nano /opt/node_reporter.py
```

**Настройки в начале файла:**

```python
# URL центрального сервера
LOG_SERVER_URL = "http://your-central-server:5000/log"

# Имя этой ноды (для логов)
NODE_NAME = "finland-1"

# Путь к access логу Xray
XRAY_LOG_PATH = "/var/log/xray/access.log"

# Файл для сохранения позиции чтения
STATE_FILE = "/tmp/node_reporter_state.json"

# Интервал чтения лога (секунды)
READ_INTERVAL = 60
```

**Запуск:**

```bash
python3 /opt/node_reporter.py
```

## Systemd сервисы

### Центральный сервер

```bash
sudo nano /etc/systemd/system/connection-limiter.service
```

```ini
[Unit]
Description=Remnawave Connection Limiter
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/path/to/remnawave-connection-limiter
ExecStart=/usr/bin/python3 main.py
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl daemon-reload
sudo systemctl enable connection-limiter
sudo systemctl start connection-limiter
```

### VPN нода

```bash
sudo nano /etc/systemd/system/node-reporter.service
```

```ini
[Unit]
Description=VPN Node Reporter
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/opt
ExecStart=/usr/bin/python3 /opt/node_reporter.py
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl daemon-reload
sudo systemctl enable node-reporter
sudo systemctl start node-reporter
```

## Настройка Xray логов

Убедись, что в конфиге Xray включены access логи с email:

```json
{
  "log": {
    "access": "/var/log/xray/access.log",
    "loglevel": "info"
  }
}
```

Формат лога должен быть примерно такой:
```
2025/12/07 15:02:32.056701 from 178.176.86.81:16708 accepted tcp:www.google.com:443 [...] email: user_848055128
```

## API эндпоинты

Центральный сервер предоставляет:

| Метод | Путь | Описание |
|-------|------|----------|
| POST | `/log` | Принимает один лог |
| POST | `/log_batch` | Принимает пачку логов (используется по умолчанию) |
| GET | `/health` | Health check |

**POST /log:**
```json
{
    "user_email": "user_848055128",
    "ip_address": "178.176.86.81",
    "node_name": "finland-1"
}
```

**POST /log_batch:**
```json
{
    "connections": [
        {"user_email": "user_123", "ip_address": "1.2.3.4", "node_name": "finland-1"},
        {"user_email": "user_456", "ip_address": "5.6.7.8", "node_name": "finland-1"}
    ]
}
```

## Как работает лимит устройств

- Лимит берется из поля `hwidDeviceLimit` юзера в Remnawave
- Если `hwidDeviceLimit = 0` или `null` — лимит не применяется (безлимит)
- Если `hwidDeviceLimit = 1` — разрешено только 1 устройство (1 IP)
- Если `hwidDeviceLimit = 2` — разрешено 2 устройства и т.д.

## Логи

Скрипт выводит информацию о своей работе:

```
[START] Connection checker started
[CYCLE] Check at 2025-12-07 15:02:32
[CYCLE] Checking 15 active users
[CHECK] User user_848055128: 2 IPs, limit: 1
[VIOLATION] User user_848055128 has 2 IPs, limit: 1
[VIOLATION] IPs: ['178.176.86.81', '95.46.197.94']
[BLOCKED] User user_848055128 for 120s
[UNBLOCK] Unblocking user user_848055128
[UNBLOCKED] User user_848055128 is now active
[CLEANUP] Deleted 150 old connection records
[STATS] DB: 234 connections, 1 blocked
```

## Telegram уведомления

Бот отправляет уведомления в таком формате:

```
⚠️ Warning: User user_848055128 has 7 active ips. {'92.36.16.186', '213.87.157.152', '178.178.92.123', '188.170.82.143', '188.19.171.46', '92.36.8.6', '94.25.235.162'}

🔴 Disabled user: user_848055128

🟢 Enabled user: user_848055128
```

### Настройка бота

1. Создай бота через [@BotFather](https://t.me/BotFather) и получи токен
2. Напиши боту `/start` чтобы активировать диалог
3. Узнай свой Telegram ID через [@getmyid_bot](https://t.me/getmyid_bot) — напиши ему `/start` и он покажет твой ID
4. Укажи токен и ID в `config.py`:
   ```python
   TELEGRAM_BOT_TOKEN = "123456:ABC-DEF..."
   TELEGRAM_CHAT_ID = "123456789"  # твой личный ID
   ```

Бот будет отправлять уведомления тебе в личку.

Если оставить `TELEGRAM_BOT_TOKEN` пустым — уведомления будут отключены.

## Troubleshooting

**Ноды не отправляют логи:**
- Проверь доступность центрального сервера: `curl http://server:5000/health`
- Проверь путь к логам Xray
- Проверь что логи пишутся: `tail -f /var/log/xray/access.log`

**Юзеры не блокируются:**
- Проверь API токен в `config.py`
- Проверь что `hwidDeviceLimit` установлен у юзера в панели
- Посмотри логи скрипта на ошибки

**Ошибки API:**
- Убедись что URL панели правильный (без `/api` на конце)
- Проверь что токен валидный и имеет права на управление юзерами

## License

MIT
